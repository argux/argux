# argux.conf
#
# This is the argux system-wide configuration file.
# See argux.conf(5) for more information.

# db_type:
db_type = sqlite3

db_path = /tmp/argux.db

db_host =
db_host = 127.0.0.1

db_port = 

autoregister_host = true

principal_provider = db

Bind port
# http_port = 8888
